/*
The SQL command is used to retrieve all records from the employees table
in a database. This command selects every column and every row from the
table, giving you a complete view of all the data stored in employees.
*/
SELECT * FROM employees;

/*
The SQL command retrieves two specific columns, department_id and location_id,
from the departments table in a database.
*/
SELECT department_id, location_id FROM departments;

